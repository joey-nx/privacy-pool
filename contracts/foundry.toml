[profile.default]
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 1

# Allow reading proof fixtures from circuits/target/proof/
fs_permissions = [{ access = "read", path = "../circuits/target/proof" }]
